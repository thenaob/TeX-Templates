% !TeX root = ../main.tex

% Idioma y codificación
\usepackage[spanish,shorthands=off]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Matemáticas
\usepackage{amsmath, amsthm, amssymb}

% Márgenes
\usepackage[a4paper,left=2cm,top=1.5cm,right=2cm,bottom=2cm]{geometry}

% Figuras
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{calc, positioning, arrows.meta}

% Tablas
\usepackage{booktabs}

% Listas
\usepackage{enumitem}

% Colores
\usepackage{xcolor}

% \usepackage{pythontex}

% Fuentes más limpias
%\usepackage{lmodern}

% Hipervínculos
\usepackage{hyperref}
\hypersetup{
	colorlinks = true,
	linkcolor = blue,
	urlcolor = teal,
	citecolor = red
}

% Tikz
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% Bibliografía
%\usepackage{cite}
%\usepackage[numbers]{natbib}
